import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "figure.play")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Hello, LogicSage Playground Template!")
        }
    }
}
